# Restaurant Menu Display

A React app showcasing a restaurant menu with reusable components and list rendering, built using Vite.

## Components
- **MenuItem.jsx** – Reusable component that accepts props and displays menu items along with a vegetarian badge.

## Run Locally
1. Clone the repository  
2. Run `npm install`  
3. Run `npm run dev`  
